{
  "name": "zclock-backend",
  "version": "2.0.0",
  "description": "Zclock backend — Firebase + Last.fm sync",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}